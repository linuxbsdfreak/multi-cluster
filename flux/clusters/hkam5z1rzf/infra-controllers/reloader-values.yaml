---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: reloader
  namespace: reloader
spec:
  chart:
    spec:
      version: '>=0.10.5'
  test:
    enable: false
  values:
    reloader:
      deployment:
        replicas: 2
